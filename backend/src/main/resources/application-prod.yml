application:
  auth:
    expose-reset-preview: false
    expose-session-token: false
    cookie:
      secure: true
      same-site: Lax
  security:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:https://app.euprocuro.com}
